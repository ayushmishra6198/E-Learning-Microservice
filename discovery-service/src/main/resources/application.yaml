spring:
  application:
    name: discovery-service

#Configuration to not to register with Server itself

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761

